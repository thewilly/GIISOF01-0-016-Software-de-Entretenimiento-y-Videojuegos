// Represents a layer in the game. A game can have multiple layers.
class Layer {

	// Constructor for every layer of the game.
	constructor() {}

	// Updates the layer at each iteration of the game.
	update() {}

	// Computes the drawing logic of the layer.
	draw() {}

	// Computes at which button the event occurs.
	onKeyPressed(pulsaciones) {}

	// Processes the controls at the layer.
	porcessControls() {}

}
